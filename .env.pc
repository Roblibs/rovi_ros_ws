ROVI_ENV=pc
# NOTE: systemd EnvironmentFile does not expand $HOME; keep this absolute if used by services.
ROVI_ROS_WS_DIR=/home/wass/dev/rovi_ros_ws

# Keep PC simulation/local tools from joining the robot DDS graph by default.
ROS_LOCALHOST_ONLY=1
